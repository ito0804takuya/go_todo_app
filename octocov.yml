# octocovの設定ファイル
coverage:
  paths:
    - coverage.out
codeToTestRatio:
  code:
    - '**/*.go'
    - '!**/*_test.go'
  test:
    - '**/*_test.go'
testExecutionTime:
  if: true
comment:
  # PR作成時のjobの場合は、PRにコメント（レポートを表示）
  if: is_pull_request